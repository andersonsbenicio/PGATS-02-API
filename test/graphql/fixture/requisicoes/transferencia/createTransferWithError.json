[
  {
    "nomeDoTeste": "Saldo insuficiente",
    "createTransfer": {
      "query": "mutation CreateTransfer($from: String!, $to: String!, $amount: Float!) { createTransfer(from: $from, to: $to, amount: $amount) { date from to amount } }",
      "variables": {
        "from": "eustaquio",
        "to": "crispiniana",
        "amount": 10000.01
      }
    },
    "mensagemEsperada": "Saldo insuficiente"
  },
  {
    "nomeDoTeste": "Remetente não encontrado",
    "createTransfer": {
      "query": "mutation CreateTransfer($from: String!, $to: String!, $amount: Float!) { createTransfer(from: $from, to: $to, amount: $amount) { date from to amount } }",
      "variables": {
        "from": "firmina",
        "to": "crispiniana",
        "amount": 15
      }
    },
    "mensagemEsperada": "Usuário remetente ou destinatário não encontrado"
  },
  {
    "nomeDoTeste": "Destinatário não encontrado",
    "createTransfer": {
      "query": "mutation CreateTransfer($from: String!, $to: String!, $amount: Float!) { createTransfer(from: $from, to: $to, amount: $amount) { date from to amount } }",
      "variables": {
        "from": "eustaquio",
        "to": "firmina",
        "amount": 15
      }
    },
    "mensagemEsperada": "Usuário remetente ou destinatário não encontrado"
  }
]
